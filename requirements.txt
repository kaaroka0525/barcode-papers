# Vercel 서버리스(웹) 배포용 — 가벼운 의존성만.
# 무거운 라이브러리(PyMuPDF, impact_factor, gemini SDK)는 서버리스에서 제외하고,
# 기능은 자동으로 우아하게 축소됨(figure 생략, IF는 OpenAlex 지표, Gemini는 REST 호출).
fastapi>=0.110
jinja2>=3.1
sqlalchemy>=2.0
psycopg2-binary>=2.9
authlib>=1.3
itsdangerous>=2.1
python-multipart>=0.0.9
requests>=2.28
PyYAML>=6.0
python-dotenv>=1.0
deep-translator>=1.11
